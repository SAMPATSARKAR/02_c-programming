#include <stdio.h>
#include <math.h>

int main(){
    double A = sqrt(9); // format-1f
    double B=pow(2,4);
    int C = round(3.14);// d 
    int D = ceil(3.1);//d
    int E = floor(3.99);//d

    double F = fabs(-100);  //f    ;
    double G = log(3);//f
    double H = sin(45); //f
    double I = cos(45);//f
    double J = tan(45);//f

    printf("\n%d",D);

    return 0;
}